[pytest]
testpaths = tests
addopts = -m "not gpu and not slow and not integration and not invariants" -q
markers =
    gpu: needs a GPU and model weights
    slow: takes more than a few seconds
    integration: runs a real pipeline stage end to end
    invariants: checks the paper's goals against real results/<hash>/ dirs
